// Primary Constructors
public class Person(string name) // only a default constructor, but it can change
{
    // can change into private filed(NOT REALDONLY- we can still change it) if u write it like this
    public void Test()
    {
        name = "";
        Console.WriteLine(name);
    }
}

// Collection Expression
// Ref Readonly Parameters
// Default Lambda Parameters
// Alias Any Type
// Inline Arrays
// Experimental Attribute
// Interceptors
